import { useTitle } from "../../CustomHook/CustomHook";

const Blogs = () => {
    useTitle("Blogs")
    return (
        <div>
           <p>Blogs are coming</p> 
        </div>
    );
};

export default Blogs;